import "reflect-metadata";
// import { CustomDecorator, SetMetadata } from "@nestjs/common";

// export const GROUP_KEY = "groups";
// export function Groups(...groups: string[]): CustomDecorator<string> {
// 	return SetMetadata(GROUP_KEY, groups);
// }
